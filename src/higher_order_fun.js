// higher order function takes function as parameter
// example of higher order
let numbers=[1,2,3,4];
numbers=numbers.map(x => x*2);
console.log(numbers);

setTimeout(() => console.log("hello") ,20);